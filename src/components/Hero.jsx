import React from "react";

const Hero = () => {
  return (
    <section
      id="hero"
      style={{
        height: "100vh",
        background: "lightblue",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <h1>Welcome to My Portfolio</h1>
    </section>
  );
};

export default Hero;
